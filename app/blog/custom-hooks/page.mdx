import { CounterDemo } from "./counter-demo";

export const metadata = {
  title: "A Quick Guide to Custom Hooks in React",
  description:
    "Learn how to create and use custom hooks to simplify your React components",
};

# Custom Hooks in React

Custom hooks allow you to extract component logic into reusable functions. Let's see how to create and use a custom hook to manage a counter. You can learn more about custom hooks in the [official React documentation](https://react.dev/learn/reusing-logic-with-custom-hooks).

<Spacer size="medium" />

### Creating a Custom Hook

Let's create a `useCounter` hook:

```ts
import { useState } from "react";

export function useCounter(initialValue = 0) {
  const [count, setCount] = useState(initialValue);

  const increment = () => setCount((prev) => prev + 1);
  const decrement = () => setCount((prev) => prev - 1);
  const reset = () => setCount(initialValue);

  return { count, increment, decrement, reset };
}
```

This hook encapsulates the state and functions for a counter.

<Spacer size="medium" />
### Using the Custom Hook

Now, let's use our `useCounter` hook in a component:

```tsx
import React from "react";
import { useCounter } from "./useCounter";

export function Counter() {
  const { count, increment, decrement, reset } = useCounter(0);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={increment}>Increment</button>
      <button onClick={decrement}>Decrement</button>
      <button onClick={reset}>Reset</button>
    </div>
  );
}
```

<Spacer size="medium" />
### Demo

Here's a demo of the counter:

<CounterDemo />

<Spacer size="medium" />

### Benefits of Custom Hooks

<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
    <h4 className="font-bold mb-2">Reusability</h4>
    <p>Easily share logic across multiple components</p>
  </div>
  <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
    <h4 className="font-bold mb-2">Simplicity</h4>
    <p>Keep components focused on rendering, moving complex logic into hooks</p>
  </div>
  <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
    <h4 className="font-bold mb-2">Testability</h4>
    <p>Isolate and test logic independently from components</p>
  </div>
  <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
    <h4 className="font-bold mb-2">Composition</h4>
    <p>Combine multiple hooks to create more complex functionality</p>
  </div>
  <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
    <h4 className="font-bold mb-2">Abstraction</h4>
    <p>Hide implementation details and provide a clean API</p>
  </div>
  <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
    <h4 className="font-bold mb-2">Consistency</h4>
    <p>Enforce consistent patterns across your application</p>
  </div>
</div>

<Spacer size="medium" />

### Conclusion

Custom hooks in React make it easy to reuse logic across components. They help keep your code organized, maintainable, and efficient. By extracting common logic into custom hooks, you can simplify your components and make your code more modular.

<Spacer size="medium" />

Happy coding! 🚀
